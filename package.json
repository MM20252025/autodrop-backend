{
  "name": "autodrop-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "license": "ISC"
}
const express = require("express");
const app = express();

const PORT = process.env.PORT || 3000;

app.get("/", (req, res) => {
  res.send("AutoDrop backend działa 🛠️");
});

app.listen(PORT, () => {
  console.log(`Server is running on port ${PORT}`);
});
"scripts": {
  "start": "node index.js"
}