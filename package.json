const http = require('http');

const PORT = process.env.PORT || 10000;

const server = http.createServer((req, res) => {
  res.end('AutoDrop backend działa!');
});

server.listen(PORT, () => {
  console.log(`AutoDrop backend działa na porcie ${PORT}`);
});